import { IncomeExpenditureItem } from './income-expenditure-item.model';

describe('IncomeExpenditureItem', () => {
  it('should create an instance', () => {
    expect(new IncomeExpenditureItem()).toBeTruthy();
  });
});
